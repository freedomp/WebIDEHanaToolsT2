{
	"name" : "sap.watt.common.plugin.preview.service.ChePreviewAdapter",
	"description" : "Che preview adapter has to be provided by the underlying backend connectivity plugin. It's responsibility is to calculate the URL of the preview environment",
	"methods" : {
		"getPreviewUrl" : {
			"params" : [{
				"name" : "oDocument",
				"type" : "sap.watt.common.plugin.document.Document"
			}],
			"returns" : {
				"type" : "object",
				"description" : "An URI object"
			}
		}
	}
}