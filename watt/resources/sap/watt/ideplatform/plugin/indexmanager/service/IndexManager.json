{
	"name" : "sap.watt.common.plugin.indexmanager.service.IndexManager",
	"description" : "The IndexManager interface",
	"methods" : {
		"getDependentIndexes": {
			"params":[
				{
					 "name"		: "docFullPath",
					 "type"		: "string",
					 "description": "the full path of the current editing document"
				},
				{
					"name"		: "astRoot",
					 "type"		: "object",
					 "description": "the ast tree of the current editing document"
				}
			],
			"returns"		: "[object]",
			"description"	: "get the array of dependent index json object for given document" 
		},
		"getDependentModules": {
			"params": [
				{
					"name": "oDocument",
					"type": "object",
					"description": "the document that other modules depend on"
		        }
		      ],
			"returns": "[object]",
			"description": "An array of dependent modules"
		},
		"hasDependentModules": {
			"params": [
				{
					"name": "oDocument",
					"type": "object",
					"description": "the document that other modules depend on"
		        }
		      ],
			"returns": "boolean",
			"description": "True if other modules depend on this one"
		},
		"getUI5ProjectInfo" : {
			"params": [
				{
					"name": "sDocumentPath",
					"type": "string"
		        }
		     ],
			"returns": "object"
		}
	},
	"configurationProperties" : {
		"cache" : {
			"type" : {
				"host" : "string"
			},
			"multiple" : false
		},
		"dependencies" : {
			"type" : {
				"category" : "string",
				"identifier" : "string"
			},
			"multiple" : true
		},
		"summaryFromMetaProvider": {
			"type" : "sap.watt.common.service.ide.SummaryFromMetaProvider",
			"multiple" : false
		}
	}
}