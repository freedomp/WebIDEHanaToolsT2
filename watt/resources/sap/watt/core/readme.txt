core layer:
Include the plugin framework - runtime engine that dynamically discovers and loads plugins.
For example: Bootstrap, plugin service (include proxies, registration, interfaces and events), configuration management, debug hook. 